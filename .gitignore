.claude/
*.env
.env.local

# PWA 빌드 산출물 (GitHub Actions에서 자동 생성)
pwa/dist/
pwa/dist-game/
pwa/node_modules/
# 루트 node_modules (msedge-tts 등 스크립트용 — 실수 커밋 방지)
node_modules/

# Worker
worker/node_modules/
worker/.wrangler/
.wrangler/
# 로컬 wrangler dev secrets — 절대 커밋 금지
.dev.vars
worker/.dev.vars
**/.dev.vars

# 일반 시크릿 패턴
*.pem
*.key
secrets.json

# 외부 프로젝트/스킬 (별도 관리, 본 repo 추적 대상 아님)
claude-workspace-dashboard-template/
stitch-skills/
supanova-design-skill/
taste-skill/

# Worker 빌드 산출물 검증용
worker/dist-check/

# 임시 작업/QA/MCP 산출물 (커밋 대상 아님 — VS Code Git 과다변경 경고 방지)
.tmp_qa/
.tmp_game/
.playwright-mcp/

# 루트에 쌓이는 개발 중 스크린샷 (서브폴더 이미지는 영향 없음)
/*.png
/*.jpeg
/*.jpg
